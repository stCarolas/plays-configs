conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
	default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    use_xft = true,
    font = 'Inconsolata for Powerline Nerd Font Complete Mono:size=14',
    gap_x = 120,
    gap_y = 80,
    minimum_height = 100,
	minimum_width = 650,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_stderr = false,
    extra_newline = false,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'override',
    own_window_transparent = true,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    show_graph_scale = false,
    show_graph_range = false
}

conky.text = [[
                ${color}NeoVim

 ${color}zo ${alignr}Open one fold under the cursor.
 ${hr}
 ${color darkgray}zO ${alignr}Open one fold under the cursor recursively.
 ${hr}
 ${color}zc ${alignr}Close one fold under the cursor.
 ${hr}
 ${color darkgray}zC ${alignr}Close all folds under the cursor recursively.
 ${hr}
 ${color}za ${alignr}Toogle fold.
 ${hr}
 ${color darkgray}zA ${alignr}Toggle folds recursively.
 ${hr}
 ${color}zv ${alignr}View cursor line.
 ${hr}
 ${color darkgray}zx ${alignr}Update folds: Undo manually opened and closed folds.
]]
